# ── Akira OpenClaw Agent — Environment Configuration ──────────────────────────
# Copy this file to .env and fill in your values.
# NEVER commit .env to version control.

# ── Deployment targets ─────────────────────────────────────────────────────────
# SSH user@IP of the VM that will run the OpenClaw container
DEPLOY_VM=youruser@YOUR_VM_TAILNET_IP

# SSH user@IP of the FreePBX/Asterisk VM (only needed for --setup-freepbx)
FREEPBX_VM=youruser@YOUR_FREEPBX_TAILNET_IP

# ── OpenClaw gateway ───────────────────────────────────────────────────────────
# Bearer token for the OpenClaw gateway API and UI.
# Generate with: openssl rand -base64 32
AKIRA_GATEWAY_TOKEN=CHANGE_ME_generate_with_openssl_rand_base64_32

# ── AI provider (at least one required) ───────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# Other supported providers (uncomment as needed):
# OPENAI_API_KEY=sk-...
# OPENROUTER_API_KEY=sk-or-...
# GEMINI_API_KEY=...

# ── Telegram (optional) ────────────────────────────────────────────────────────
# Get a bot token from @BotFather on Telegram
AKIRA_TELEGRAM_BOT_TOKEN=1234567890:AAAA...
# Your Telegram user ID (find it via @userinfobot)
AKIRA_TELEGRAM_CHAT_ID=YOUR_TELEGRAM_USER_ID

# ── Phone call relay (optional) ────────────────────────────────────────────────
# URL of the FreePBX relay (running on your FreePBX VM)
FREEPBX_RELAY_URL=http://YOUR_FREEPBX_TAILNET_IP:18511
# Shared secret between the agent and the relay.
# Generate with: openssl rand -hex 20
FREEPBX_RELAY_TOKEN=CHANGE_ME_generate_with_openssl_rand_hex_20
# Default phone number to call for alerts (E.164 format)
OWNER_PHONE=+1234567890

# ── Data paths on the VM ───────────────────────────────────────────────────────
# Where persistent OpenClaw state is stored on the VM
DEPLOY_DIR=/home/youruser/akira
DATA_DIR=/home/youruser/akira-data

# ── Agent domain ───────────────────────────────────────────────────────────────
# The DNS name used in nginx vhost and the Headscale extra_records entry.
# Must resolve to your VM's tailnet IP (add to Headscale extra_records).
AGENT_DOMAIN=akira.yourdomain.com
